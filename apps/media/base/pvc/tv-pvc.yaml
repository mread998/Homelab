apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-sonarr-tv
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: ""
  selector:
    matchLabels:
      tier: tv     
---