apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: movies-pvc
  namespace: media-server 
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi         # ≤ PV capacity; purely for matching
  volumeName: nfs-pv-movies  # <-- direct bind to the PV (static provisioning)
  storageClassName: ""      # important: empty for static PVs
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tv-pvc
  namespace: media-server 
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi         # ≤ PV capacity; purely for matching
  volumeName: nfs-pv-tv  # <-- direct bind to the PV (static provisioning)
  storageClassName: ""      # important: empty for static PVs
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: music-pvc
  namespace: media-server 
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi         # ≤ PV capacity; purely for matching
  volumeName: nfs-pv-music  # <-- direct bind to the PV (static provisioning)
  storageClassName: ""      # important: empty for static PVs
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: books-pvc
  namespace: media-server 
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi         # ≤ PV capacity; purely for matching
  volumeName: nfs-pv-books  # <-- direct bind to the PV (static provisioning)
  storageClassName: ""      # important: empty for static PVs
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: learn-pvc
  namespace: media-server 
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi         # ≤ PV capacity; purely for matching
  volumeName: nfs-pv-learn  # <-- direct bind to the PV (static provisioning)
  storageClassName: ""      # important: empty for static PVs
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-pvc
  namespace: media-server 
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi         # ≤ PV capacity; purely for matching
  volumeName: nfs-pv-config  # <-- direct bind to the PV (static provisioning)
  storageClassName: ""      # important: empty for static PVs
---

